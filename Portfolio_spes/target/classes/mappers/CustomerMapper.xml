<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spes.mapper.CustomerMapper">

	<!-- 카테고리 조회
	<select id="qcategory" resultType="com.speshase.model.CustomerVO">
	<![CDATA[
		select * from questionsList where qcategory=#{qcategory}
	]]>
	</select> -->
	
	<!-- 카테고리 전체 조회 -->
	<select id="qcategoryList" resultType="com.spes.model.CategoryVO">
	<![CDATA[
		select * from questionsList order by qno desc;
	]]>
	</select>

</mapper>