<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spes.mapper.CustomerMapper">

	
	<!-- 자주묻는 질문 + 조회 -->
	<select id="qcategoryList" resultType="com.spes.model.CategoryVO">
	<![CDATA[
		select * from questionsList order by qno desc
	]]>
	</select>
	
	<!-- 자주묻는 질문 + 글쓰기 -->
	<insert id="qestionisWrite">
	<![CDATA[
		insert into questionsList (qcategory, qcategoryk, qsubject, qcontent)
		values (#{qcategory}, #{qcategoryk}, #{qsubject}, #{qcontent})
	]]>
	</insert>

</mapper>